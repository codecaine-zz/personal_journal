{
  "name": "personal_journal",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "dev": "electrobun build --env=stable && ./build/stable-macos-$(uname -m | sed 's/x86_64/x64/')/Personal\\ Journal.app/Contents/MacOS/launcher",
    "build": "electrobun build --env=stable"
  },
  "type": "module",
  "dependencies": {
    "electrobun": "^1.18.1"
  }
}
